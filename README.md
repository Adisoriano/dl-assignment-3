# dl-assignment-3
This repository contains the solution for BGU Deep Learning Assignment 3. It extends a single hidden layer neural network to two hidden layers and applies it to the MNIST dataset, evaluating performance with macro AUC and comparing against baseline and PyTorch models.
